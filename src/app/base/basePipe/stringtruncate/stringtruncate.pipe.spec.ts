import { PipenamePipe } from './stringtruncate.pipe';

describe('PipenamePipe', () => {
  it('create an instance', () => {
    const pipe = new PipenamePipe();
    expect(pipe).toBeTruthy();
  });
});
