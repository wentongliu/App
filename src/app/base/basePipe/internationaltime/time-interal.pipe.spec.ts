import { TimeInteralPipe } from './time-interal.pipe';

describe('TimeInteralPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeInteralPipe();
    expect(pipe).toBeTruthy();
  });
});
